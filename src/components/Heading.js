import React from 'react'

const Heading = ({headingText}) => {
    return (
        <h1 className="text-white text-center text-capitalize " data-aos="fade-up">{headingText}</h1>
    )
}

export default Heading;
